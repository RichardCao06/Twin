__pycache__/
*.pyc
.DS_Store
.pytest_cache/
*.egg-info/
.venv/
state.db
# Litter from running the CLI as a script with a misconfigured DWS_AGENT_HOME
<__main__.Paths object at *>
tests/mock/__pycache__/
# IDE
.idea/
# 文档渲染产物（html 由 md 生成，归档只留 md）
docs/**/*.html
# Playwright E2E 验收的临时产物（截图/快照/storageState，含登录态，绝不入库）
.playwright-mcp/
/*.png
# uat E2E 凭证（含密码，放项目根但绝不入库；切勿 git add -f）
/uat.env

# 生产凭证(含测试账号密码/token)，绝不入库
prod.env
