"""core subpackage: config, paths, scaffold, crypto.

These are the lowest-level building blocks; every other module imports the
path layout and config from here so the home-directory layout is defined in
exactly one place.
"""
