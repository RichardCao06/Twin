# 复盘 2026-06-24：从李辰一张截图，到派出根因修复 + 照出部署瓶颈

> 承 [复盘-2026-06-23](复盘-2026-06-23.md)。本日主线：李辰反馈「编辑器没强退、在报错」→ 建验收能力 → 手动部署 → E2E 真验收发现 #71 假性修复 → 派根因 task。

## 一、主线与结果

| # | 环节 | 做了什么 | 状态 |
|---|---|---|---|
| 1 | 问题发现 | 读李辰单聊、下载报错截图，识别「被顶下线没强退」 | ✅ 手动 |
| 2 | 初诊 | 定位 #71（4011 拦截器）已合但 uat2 未部署 | ✅ |
| 3 | 建验收能力 | 落地 `uat-verify` subagent + `uat_login.py` + 环境映射 + 验收标准（四级判定/报告模板） | ✅ `349a556`/`a503bc9` |
| 4 | 诊断"自动部署坏在哪" | 查实：CI build 超时(PR#2 没合) + reusable 只 build+push 无 k8s deploy + 明文凭证泄露 | ✅ |
| 5 | 手动部署 | 本地 `build:test`+`buildx push` → 推对 harbor + 重启 → editor2 含 #71 | ⚠️ 成功但踩 registry 错配 |
| 6 | E2E 真验收 | 脚本登录 → 注入登录态 → 双 session 顶下线 → 观察前端 | ✅ 跑通 |
| 7 | **关键发现** | #71 没解决！业务接口返 `500`≠`4011`（只 SSO 返 4011），前端接不到 → 满屏系统错误不强退 | ✅ 高价值 |
| 8 | 派修复 | 建+publish dataset 后端 4011 task（`61af3d2e`） | ✅ worker 执行中 |

## 二、实打实跑通的（有证据，非"应该能"）
- **uat-verify E2E 全链**在真 uat2 跑通（非 mock）：B 踢 A，A token 调 SSO 返 `code=4011`「账号已在别处登录」；
- **手动部署生效**：editor2 嗅探 `has_4011_key` false→true、zhKeyCount 1653→1655；
- **真验收抓到代码审查发现不了的缺陷**：业务接口 `{"code":500,"msg":"系统错误"}`（7 接口全 500）——光看 #71 代码/SSO 层会误以为修好了；
- 3 commit + 1 publish task + `uat_login` 修复（mock 测过）。

## 三、踩的坑 / 暴露的问题
- **推错 registry**：editor2 吃 `db-uat2`(harbor)，先推天翼云(`hiqlcd-app-uat2`)没生效 → 映射没文档、靠现摸（已修：§12）；
- **登录态注入踩 4 坑**：`require` 不可用 / `filename` 要项目内 / cookie 要 URL 编码 / 缺 `accessToken` cookie → `uat_login` 首版不完整（已修）；
- **kubeconfig 只读**：`patch/update deployment`=no，agent 不能 rollout，部署最后一步必须人工；
- **顶下线测试踢真人**：hiqadmin 共用；
- **#71 假性修复**：方向对一半，前提（业务接口返 4011）不成立，真刀在 dataset 后端（已派 task）。

## 四、"发现→解决"全自动化：成熟度 + 最大缺口

理想链：`发现 → 诊断 → 建task → 改+测 → PR → 合并 → 部署 → 验收 → 回报`

| 环节 | 成熟度 | 缺什么 |
|---|---|---|
| 发现 | ⚠️ 半自动 | feedback 巡检有；钉钉靠手动看；图片要手动下载理解 |
| 诊断根因 | ⚠️ 半自动 | impact/diagnose + 会话内；这次靠多轮手动 debug |
| 建 task | ✅ | `dws-agent task create` |
| 改+测+PR | ✅ | worker + 测试门禁（需 publish 放行） |
| 合并 | ⬜ 人工 | 人 review（应保留） |
| **部署** | 🔴 **最大断点** | CI 坏（PR#2 没合 + 无 k8s deploy）；手动多步；映射靠摸；kubeconfig 只读不能 rollout |
| 验收 | ⚠️ | uat-verify 建了；但登录态注入复杂、踢真人、部署完要人喊才验 |
| 回报 | ⚠️ | announce 有；这次没回报李辰 |

**整条闭环卡在「部署」**——#71 代码早改好，部署断着，今天大半精力耗在打通部署。

## 五、优化建议与落实状态

| # | 建议 | 优先级 | 状态 |
|---|---|---|---|
| 1 | 修 CI（合 PR#2）+ reusable 补 k8s deploy/ArgoCD | 🔴 治本 | ⏸ **暂缓（无权限改 workflow / k8s rollout）** |
| 2 | 落地 `dws-agent deploy`（应急可控、只到 push） | 🟠 | ✅ `.claude/agents/uat-deploy.md` |
| 3 | 部署→验收自动串联 | 🟠 | ✅ 并入 uat-deploy 工作流（push+重启后接 uat-verify） |
| 4 | uat 专属测试账号（替 hiqadmin 共用） | 🟡 | 📋 action（需人申请；agent 护栏已标注临时性） |
| 5 | registry/deployment 映射文档化 | 🟡 | ✅ `uat-环境清单 §12` |
| 6 | 明文凭证轮换 + 移 Secrets | 🟡 安全 | 📋 action（需人轮换 + 改 workflow；清单 §12b 已标） |
| 7 | 发现端增强（钉钉巡检 + 截图理解） | 🟢 | 📋 设计方向（后续落地） |

> 落实说明：**2/3/5 已落成实体**（agent + 文档）；**4/6 是人工动作**（申请账号 / 轮换凭证），我落实到"文档化 + action + 护栏标注"，实际执行需人；**7** 是后续工程（避免半成品），先记方向。

## 六、待办 action items
- [ ] **(1)** 有权限后：合 `HiQ-AI/workflow#2` 修 CI 超时 + 给 reusable 加 k8s deploy（或对接 ArgoCD imageUpdater）。
- [ ] **(4)** 申请 uat 专属测试账号，替掉共用 hiqadmin（免踢真人、验收随时可跑）。
- [ ] **(6)** 轮换 reusable workflow 明文凭证（天翼云密码 / GitHub PAT / 钉钉 token）+ 移 GitHub Secrets。
- [ ] **(7)** `feedback_patrol` 扩展钉钉源 + 截图自动理解。
- [ ] **闭环收尾**：dataset 后端 4011 task（`61af3d2e`）完成 → 部署 db-uat2(harbor) → `uat-verify` 复测 → 回报李辰。

## 七、一句话
验收能力（`uat-verify`）从 0 到 1 建成并真跑出价值（揪出 #71 假性修复），部署能力（`uat-deploy`）也固化成型；但全链真正的瓶颈是**部署自动化**——治本要等改 CI/k8s 的权限。
