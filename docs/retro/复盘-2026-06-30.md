# 复盘 · 2026-06-30

> 一句话：**1 个生产 bug 追到根（4 层套娃）+ 3 轮镜像迭代 + 1 套 800 条数据交付（关系 Excel + IPCC LCIA）+ 1 次假性 PASS 引发的生产事故复盘 + 6 条 memory 沉淀。**

---

## 输出物清单

| 类型 | 数量 | 详情 |
|---|---|---|
| 生产 PR | 3 | [HiQ-AI/dataset#112](https://github.com/HiQ-AI/dataset/pull/112) (合) / [#113](https://github.com/HiQ-AI/dataset/pull/113) (合) / [#114](https://github.com/HiQ-AI/dataset/pull/114) |
| 生产镜像 | 3 | `registry.cn-sh1.ctyun.cn/hiq-ai/dataset:v20260629-1/-2/-3` |
| 代码改动 | 5 文件 | TenantHandler / IlcdExportAsyncCoordinator / ImportAsyncCoordinator / VersionTableNameHandler / IlcdExportContextGuard + 2 × Actor |
| 交付 Excel | 2 | 南网-超高压项目数据（sheet1 元数据 + sheet2 6664 关系边）+ IPCC 2021 LCIA（800×39） |
| 验证脚本 | 2 | `export_nwcgy_lcia.py` + `verify_nwcgy_export.py`（27 项 PASS） |
| memory 沉淀 | 6 条 | 跨会话生效的反模式教训 |

---

## 时间线

```
14:00  📍 dataset 导出 bug 排查
       ├ 用户报 uat2 HiQLCD 0.2.267 导出空
       ├ 跟随 hotfix commit msg → PR #111 镜像验证仍空 ❌（吃 30min）
       ├ PG 直查发现 hiq_editor 6 条 / hiq_background_db 0 条
       ├ PR #112 v20260629-1 → uat2 "通过"（假性）→ 合 main → 发线上
       ├ 生产 NW_CGY 0.0.111 爆 → 直查生产 PG → PR #113 → v20260629-2
       └ 17:17 Jackson UnrecognizedPropertyException → PR #114 → v20260629-3

17:00  📍 张鹏聊天分析 + 大后台数据库套餐探查
       ├ 张鹏要求拆解：① 800 ILCD 包（去 GWP） ② Excel 关系（datasets 级别）
       └ 数据库套餐下拉规则锁定：ts_datasource_info.saleable=true

18:00  📍 改 testExportHiQALUPR 脚本 → NW_CGY 0.0.110 海科租户
       ├ 拿 4 个关键 ID + 发现"海科租户 code=hiq"（脚本不用改 schema）
       ├ Python 改写：去 GWP + UprInfoExcel 流级 → DatasetRelationExcel 数据集级
       └ 800 process + 6664 边 Excel 落 Downloads

20:00  📍 验证 + LCIA 导出
       ├ verify 脚本 27 项 PASS（零差异）
       ├ 拆解 8397→6184 差异（4 步过滤）
       ├ 处理 1048 avoided burden（用户选不纳入）
       └ LCIA 导出：IPCC 2021 method，32 indicator × 800 process

穿插   📍 dataset-sso uat1 镜像构建（卡在 Docker daemon 未启动）
       📍 李海风消息分析（大后台另一处疑似同根 bug）
```

---

## 主线 1：dataset 导出 bug — 4 层根因套娃

| 层 | 根因 | PR | 教训 |
|---|---|---|---|
| L1 | 异步线程 `UserContext`=null → MP 拼 `WHERE tenant_id=null` → 主线程 SQL 0 行 | #112 | TenantHandler 应该 fallback 到 TenantContext |
| L2 | `VersionTableNameHandler` 看 dsKey 空 + tb_processes 在 `BACKGROUND_TABLE_LIST` → 强制 push hiq_background_db，覆盖 @DS | #112 | 公共拦截器副作用没人发现，业务表抢栈 |
| L3 | fan-out 子线程 `ContextSnapshot` 漏管 `dsKey` / `OVERRIDE_HOLDER` ThreadLocal | #113 | 异步上下文护栏应该全 ThreadLocal 覆盖 |
| L4 | Actor 类没 `@JsonIgnoreProperties(ignoreUnknown=true)`，库里历史 `type` 字段抛 UnrecognizedProperty | #114 | entity 反序列化卫生 |

### 排查的关键转折点

| 阶段 | 关键发现 | 工具 |
|---|---|---|
| 初判 | hotfix commit msg 指向"SpEL @DS 失效" | git log + commit message |
| 第一次推翻 | PR #111 镜像 push 后仍空 → commit msg 误导 | uat2 kubectl + dataset 日志 |
| 第二次锁定 | PG 直查 hiq_editor=6 条 / hiq_background_db=0 条 | psql 直查 |
| **HiQLCD 1.4.0 巧合通过** | hiq_background_db 有 31744 条已发布副本，fan-out 错路由刚好兜住 | 生产 PG 实测 |
| **NW_CGY 0.0.111 戳穿** | 未发布到 background → 错路由查空 → fan-out 全部 NPE | 生产日志 + PG |
| **Jackson 第四层** | dsKey 修了后，反序列化路径才被真正跑到 | 生产 stack trace |

### 为什么这个 bug 找了这么久

详见 [`memory/chain-root-cause-think-next-layer.md`](../.claude/projects/-Users-shujudagongren-Myspace-dingding-agent/memory/chain-root-cause-think-next-layer.md) + 之前 06-30 那次 `dataset` 仓内复盘。简而言之：**4 层独立 bug 套娃，必须修一层才能暴露下一层；每修一层都"看起来修好了"，直到下一层在生产暴露**。

---

## 主线 2：南网-超高压 800 条数据交付（张鹏需求）

### 张鹏的核心要求（聊天分析得出）

| 件 | 内容 | 注意 |
|---|---|---|
| ① 800 条 ILCD 包 | 数据本身的完整 ILCD | ❌ **不含 GWP/LCIA**（"未明确说明都不给"） |
| ② Excel 关系 | 数据集间关系（**datasets 级别**，不是 flow 级别） | 一层上游，不递归 |

附加规则：
- 上游引用 → 只给 uuid + name（不给完整数据）
- 历史曾误给 GWP，要从群里撤回旧方案消息

### sheet2 关系数据 = 6664 条边

**8397 → 6664 差异链**（4 步过滤）：

| 步骤 | 数量 | 减少 | 原因 |
|---|---|---|---|
| A 原 SQL（全 0.0.110 has-provider） | 8397 | — | 基准 |
| B + 限定海科租户 | 8397 | 0 | 0.0.110 全是海科 |
| C + 限定 800 个 process | 8397 | 0 | **0.0.110 就这 800 个 process** |
| D + `is_input=true` | 7349 | −1048 | 排除 output exchanges（avoided burden） |
| E 去重 (src→dst) | 6664 | −685 | 同 A→B 多次引用 |
| F 其中"上游在包外" | 6184 | −480 | 480 条上游在包内（标记，未丢） |

**用户选择不纳入 1048 条 avoided burden**——保持严格"A 用了 B"口径。

### 验证：27 项 PASS

| 维度 | 检查项数 | 结果 |
|---|---|---|
| A. sheet1 行集合 | 5 | PASS |
| B. sheet1 字段值（800 行 × 13 字段） | 13 | PASS |
| C. sheet2 边集合 | 4 | PASS |
| D. 跨 sheet + 上游 name | 5 | PASS |

零差异。

---

## 主线 3：LCIA 导出（IPCC 2021）

| 项 | 值 |
|---|---|
| Method | `IPCC 2021` = `f4c86198-417e-35f9-8922-f0dd04403e17` |
| 维度 | 8 cat × 平均 4 ind = **32 列** |
| 数据源 | H5 矩阵 `NW_CGY_0_0_110_{CUT_OFF,CONSEQUENTIAL}_lcia_matrix.h5` |
| 文件 | `南网-超高压项目LCIA结果_IPCC2021.xlsx` (800 × 39) |
| 实现 | 纯 Python（h5py + psycopg2 + openpyxl），不依赖 Java |

H5 结构：
- `matrix[893, 400]` — 893 row（method_cat_indicator）× 400 col（process）
- `row_uuids`: 三段 `methodId_categoryId_indicatorId`
- `col_uuids`: `processId____version`
- 单位都是 `kg CO2-Eq`（GTP/GWP 通用）

---

## 主线 4：副产物

| 副产物 | 状态 |
|---|---|
| dataset-sso uat1 镜像 | ⏸ 卡在 Docker daemon 未启动；前置 (worktree + DockerfileLocal 改 profile uat2→uat1) 已就绪 |
| 大后台数据库套餐下拉规则 | 锁定 = `ts_datasource_info.saleable=true AND is_deleted=false`（9 条候选跟截图完全吻合） |
| 李海风消息分析 | 大后台某功能"新增数据点击展示为空只有线上"——疑似同根 bug（同样 tenant ThreadLocal 问题） |

---

## 4 个吃时间最多的反模式

| # | 反模式 | 今天踩了几次 | 累计吃掉时间 |
|---|---|---|---|
| 1 | **信中间信号不看产物**（"size=6" / "task COMPLETED"） | 2 次（uat2 假性通过 + HiQLCD 1.4.0 巧合通过） | **~60 min** |
| 2 | **信前人 commit msg 根因** | 1 次（PR #111 写死 @DS 路线） | ~30 min |
| 3 | **远程 build/push 循环不停** | 5 轮才转本地 | ~40 min |
| 4 | **catch 静默吃异常** | dataset 代码里发现，靠它把 4 层 bug 全藏住数月 | bug 隐藏期 |

**这 4 个加起来吃了今天 ~2 小时**，是 4 层根因背后的元问题。

---

## 已沉淀（6 条 memory）

跨会话生效，未来再遇类似场景自动召回：

| memory 文件 | 一句话 |
|---|---|
| `verify-by-final-artifact.md` | 验收看产物，不看中间信号 |
| `chain-root-cause-think-next-layer.md` | 每次 fix 后想"下一层会爆什么" |
| `local-reproducer-over-remote-loop.md` | 3 次远程循环就停下来建本地 |
| `silent-catch-hides-bug.md` | catch 必须让任务级反映失败 |
| `dont-trust-prior-root-cause.md` | 前人的根因是猜测不是真理 |
| `prod-pg-direct-access.md` | 生产 PG 直连用 `prod.env` PROD_DB_* |

---

## 还没做完的

| # | 事 | 状态 |
|---|---|---|
| 1 | dataset-sso uat1 镜像 push | ⏸ 等 Docker Desktop 启动 |
| 2 | PR #114 review/merge + v20260629-3 部署生产 | ⏸ 等 review |
| 3 | 800 ILCD 包补全（给张鹏） | ⏸ 等 v20260629-3 上线后 editor2 触发，或用 `ilcd-export` skill |
| 4 | 李海风/李辰那个"点击展示空"bug 深挖 | ⏸ 等具体页面 + 网络请求信息 |
| 5 | dataset 工程治理：`processBatch` catch 改 fail-task；entity 统一 `@JsonIgnoreProperties` | ⏸ 跨 PR 治理 |

---

## 全天数据

```
PR 数:       3 (合 2 / 待 1)
镜像数:      3 (v20260629-1/-2/-3) + 1 (sso uat1 待)
代码改动:    ~150 行 (5 文件)
Excel 输出:  2 文件 (~1.3MB)
脚本输出:    3 个 Python (export + verify + LCIA)
PG 直查:     20+ 次 (uat2 + prod, 多库)
日志拉取:    10+ 次 (uat2 kubectl + prod ks_logs.py)
memory 增:   6 条
```

---

## 关键代码 / 工具收藏

- 生产 PG 直连：`source ./prod.env && export PGPASSWORD="$PROD_DB_PASSWORD" && psql -h "$PROD_DB_HOST" -p "$PROD_DB_PORT" -U "$PROD_DB_USER" -d hiq_editor`
- 生产日志：`source ./prod.env && python3 scripts/ops/ks_logs.py --service dataset --grep "..." --since N`
- H5 远程下载：`curl -s "http://192.168.8.8:31680/download-matrix/?database_name=<code>&database_version=<v>&database_system_model=<sm>&matrix_type=<lci|lcia>_matrix" -o file.h5`
- uat kubectl 模板：`kubectl --kubeconfig ~/Workspace/hiq-backend-admin/.claude/kubeconfig-uat.yaml --server https://192.168.8.8:6443 --insecure-skip-tls-verify -n <ns> ...`
